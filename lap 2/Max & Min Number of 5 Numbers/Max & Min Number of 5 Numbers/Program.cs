using System;

class Pick_5
{
    static void Main()
    {
        /*
        Console.WriteLine("Hello, Please type five numbers and press enter after every one to see the max and min number");

        int a = int.Parse(Console.ReadLine());
        int b = int.Parse(Console.ReadLine());
        int c = int.Parse(Console.ReadLine());
        int d = int.Parse(Console.ReadLine());
        int e = int.Parse(Console.ReadLine());

        if (a >= b && a >= c && a >= d && a >= e)
        {
            Console.WriteLine($"the max number is: {a}");
        }

        if (b >= a && b >= c && b >= d && b >= e)
        {
            Console.WriteLine($"the max number is: {b}");
        }

        if (c >= a && c >= b && c >= d && c >= e)
        {
            Console.WriteLine($"the max number is: {c}");
        }

        if (d >= a && d >= c && d >= b && d >= e)
        {
            Console.WriteLine($"the max number is: {d}");
        }

        if (e >= a && e >= c && e >= d && e >= b)
        {
            Console.WriteLine($"the max number is: {e}");
        }

        if (a <= b && a <= c && a <= d && a <= e)
        {
            Console.WriteLine($"the min number is: {a}");
        }

        if (b <= a && b <= c && b <= d && b <= e)
        {
            Console.WriteLine($"the min number is: {b}");
        }

        if (c <= b && c <= a && c <= d && c <= e)
        {
            Console.WriteLine($"the min number is: {c}");
        }

        if (d <= b && d <= c && d <= a && d <= e)
        {
            Console.WriteLine($"the min number is: {d}");
        }

        if (e <= b && e <= c && e <= d && e <= a)
        {
            Console.WriteLine($"the min number is: {e}");
        }

        */


        /*
        Console.WriteLine("Hello, Please type three numbers and press enter after every one they will be used to check a second degree equation");



        double a = double.Parse(Console.ReadLine());
        double b = double.Parse(Console.ReadLine());
        double c = double.Parse(Console.ReadLine());


        double result = b * b - 4 * a * c;



        if (result > 0)
        {
            Console.WriteLine("then x1 and x2 roots are real numbers");
        }

        if (result == 0)
        {
            Console.WriteLine("then x1 and x2 roots are equal numbers");
        }

        if (result < 0)
        {
            Console.WriteLine("then x1 and x2 roots are complex numbers");
        }

        Console.WriteLine($"result:{result}");

        */






    }
}



/*
2) second degree equation
axA2 + bx +
hag 2
xl = {—b + sqrt
user b
c
— 4 ac ) ) / 2a
A — 41 ac ) ) / 2a
Zero
c
— sqrt 2
xl and r 2 real numbers
xl x2 the 2 roots r equal
xl and are complex numbers
1
1
1
1
4
1
-s
6
*/